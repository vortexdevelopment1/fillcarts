{
  "name": "fillcarts-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node run-dev.js",
    "dev:customer": "npm --prefix customer run dev",
    "dev:vendor": "npm --prefix vendor run dev",
    "dev:rider": "npm --prefix rider run dev"
  }
}
